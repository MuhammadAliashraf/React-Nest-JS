# core-backend-utils-helpers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-backend-utils-helpers` to build the library.

## Running unit tests

Run `nx test core-backend-utils-helpers` to execute the unit tests via [Jest](https://jestjs.io).
